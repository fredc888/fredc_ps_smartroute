<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <!-- "Mark Favorite", must appear as action button if possible. -->
    <item
        android:id="@+id/action_load"
        android:icon="@android:drawable/ic_menu_mylocation"
        android:title="@string/label_load"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_more_items"
        android:title="@string/label_more_items"
        android:icon="@android:drawable/ic_menu_more"
        app:showAsAction="always">
        <menu>
        <item android:id="@+id/action_load_default"
            android:title="@string/label_load_defaults" />
        <item android:id="@+id/action_toggle_developer_mode"
            android:title="@string/label_developer_mode_turn_on" />
        </menu>
    </item>

</menu>